import 'package:flutter/rendering.dart';

class ColorPalette {
  static ColorPalette instance = ColorPalette._init();
  ColorPalette._init();

  final blackcurrant = Color(0xFF2F2937);
  final electricViolet = Color(0xFF5C00F1);
  final manatee = Color(0xFF8F939D);
  final snuff = Color(0xFFDAD0EA);
}
